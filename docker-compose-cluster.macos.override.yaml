# Override file for Apple Silicon (ARM64) Macs
# Forces x86/amd64 emulation for images that don't have ARM64 variants

services:

  carbonio-mailbox02:
    platform: linux/amd64

  carbonio-mariadb02:
    platform: linux/amd64
