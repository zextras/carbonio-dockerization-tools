apiVersion: v1
kind: Pod
metadata:
  name: carbonio-postfix
  labels:
    app: carbonio-postfix
spec:
  containers:
    - name: app
      image: registry.dev.zextras.com/dev/carbonio-mta:latest
      imagePullPolicy: Always
      env:
        - name: LDAP_HOST
          value: "carbonio-openldap"
        - name: LDAP_PORT
          value: "1389"
        - name: LDAP_ROOT_PASSWORD
          value: "qh6hWZvc"
        - name: LDAP_ADMIN_PASSWORD
          value: "password"
      ports:
        - containerPort: 25
          hostPort: 20025
#
#    - name: consul-agent
#      image: docker.io/hashicorp/consul:1.17
#      args: [agent, -client=0.0.0.0, -bind=0.0.0.0, -retry-join=consul-server, -grpc-port=8502, "-hcl=acl { enabled = true default_policy = \"allow\" tokens { agent = \"00000000-0000-0000-0000-000000000000\" } }"]
#
#    - name: sidecar
#      image: registry.dev.zextras.com/dev/carbonio-mta-sidecar:latest
