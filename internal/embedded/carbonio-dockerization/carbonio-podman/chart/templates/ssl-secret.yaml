{{- if .Values.groups.ui.enabled }}
# nginx.crt + nginx.key for the composed-ui proxy. Replaces the ../ssl hostPath.
apiVersion: v1
kind: Secret
metadata:
  name: carbonio-ui-ssl
type: Opaque
data:
{{ (.Files.Glob "files/ssl/*").AsSecrets | indent 2 }}
{{- end }}
