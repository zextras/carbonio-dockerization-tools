{{- if .Values.groups.todo.enabled }}
{{- if eq .Values.platform "k3s" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbonio-tasks
  labels:
    app: carbonio-tasks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbonio-tasks
  template:
    metadata:
      labels:
        app: carbonio-tasks
    spec:
{{ include "carbonio.deployment.k3sPodDefaults" . | indent 6 }}
{{ include "carbonio.tasks.podbody" . | indent 6 }}
{{- else }}
apiVersion: v1
kind: Pod
metadata:
  name: carbonio-tasks
  labels:
    app: carbonio-tasks
spec:
{{ include "carbonio.tasks.podbody" . | indent 2 }}
{{- end }}
{{- end }}
