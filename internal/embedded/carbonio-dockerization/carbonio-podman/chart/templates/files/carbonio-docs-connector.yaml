{{- if .Values.groups.files.enabled }}
{{- if eq .Values.platform "k3s" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbonio-docs-connector
  labels:
    app: carbonio-docs-connector
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbonio-docs-connector
  template:
    metadata:
      labels:
        app: carbonio-docs-connector
    spec:
{{ include "carbonio.deployment.k3sPodDefaults" . | indent 6 }}
{{ include "carbonio.docs-connector.podbody" . | indent 6 }}
{{- else }}
apiVersion: v1
kind: Pod
metadata:
  name: carbonio-docs-connector
  labels:
    app: carbonio-docs-connector
spec:
{{ include "carbonio.docs-connector.podbody" . | indent 2 }}
{{- end }}
{{- end }}
