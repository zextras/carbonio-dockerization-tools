tokens/

# Helm packaging output
*.tgz

# Per-dev chart values override (documented as a local-only file)
chart/values.local.yaml

# Ad-hoc dev artifacts (helm template dumps, scratch output, etc.)
out.txt
