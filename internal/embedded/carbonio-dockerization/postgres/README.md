While we have packages for each database init, since they are not yet published on registry I chose to simply launch Postgres with an init script that creates necessary databases.

I'm sure we can figure how to make this more similar to a real deployment in the future.